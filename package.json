{
  "name": "eta-www",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "deploy": "astro build && wrangler pages deploy dist --project-name eta-www --branch prototyp --commit-dirty=true",
    "deploy:prod": "astro build && wrangler pages deploy dist --project-name eta-www --branch main --commit-dirty=true"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.1.4",
    "astro": "^5.6.1",
    "tailwindcss": "^4.1.4"
  },
  "devDependencies": {
    "wrangler": "^4.128.0"
  }
}
